node_modules/
dist/
.expo/
*.log
.env
.env.local
.DS_Store
tmp/
# DB seed dump for Docker migration — real user data, kept out of git (3.9MB).
# Still shipped to the new server via rsync (see deploy/README.md).
deploy/seed/*.sql.gz
# secrets / signing keys (defense-in-depth — also covered in app/.gitignore)
*.p8
*.p12
*.pem
*.key
*.mobileprovision

# iCloud-safe deps (node_modules symlinked to .nosync)
node_modules.nosync/

__pycache__/
server/node_modules

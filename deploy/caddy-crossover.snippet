# ── Crossover (additive; routes only this hostname to the crossover app) ──
# Append this block to /root/transyol/Caddyfile, then:
#   docker exec transyol-caddy-1 caddy reload --config /etc/caddy/Caddyfile
168-222-180-190.nip.io {
	reverse_proxy crossover-app-1:8080
	encode gzip
}

# Turkish type and opening polish — 2026-09-09

Source checkout: `/private/tmp/cof-ship-20260906/ui2/app`.

- Flattened composite glyph outlines in an OFL-compliant renamed derivative of Lilita One. Original font remains untouched. Existing runtime family alias is retained to update all consumers. See font notes and rebuild script in `app/assets/fonts` and `app/scripts`.
- Simulator visual comparison: `turkish-before.png` versus `turkish-flat-glyphs.png`. NFC/NFD İPTAL and Turkish alphabet samples now display centered accents; the latter screenshot includes the Metro refresh banner. `turkish-after.png` is an intermediate splash capture, not verification evidence.
- Matchmaking fact card now advances on every press, restarts the reading timer, and uses a native-driver fade. Existing football facts are unchanged. Reduced-motion mode allows manual advance without animation.
- Opening uses the supplied MIAV PNG unchanged; Crossover credit is a small single line without a panel. Native launch storyboard uses the same PNG, generated by the existing config plugin.

Validation: TypeScript passed; all 32 startup state tests passed; generated native splash storyboard compiled successfully with ibtool in an isolated temporary project.

Simulator QA was interrupted by the Mac locking: fact card was present with its button label and hint, but tap advancement and the final opening appearance were not confirmed. The offline `PolishPreview.tsx` fixture is not wired into App; the temporary development override was removed.

No deployment or OTA performed. The native splash change requires a fresh iOS build; JS/font/image changes are separate from that native requirement.

import type { ComponentType, ReactNode } from 'react';
import type { StyleProp, ViewStyle } from 'react-native';

export const GestureHandlerRootView: ComponentType<{ children?: ReactNode; style?: StyleProp<ViewStyle> }>;
